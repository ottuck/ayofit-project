<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.app.ayofit.mapper.LoginMapper">

	<select id="checkLogin" resultType="AccountDTO">
		select l_id, l_email, l_password, l_name, l_info, l_google, a_age, a_gender, a_height, a_weight, a_activity
		from login, accounts
		where l_info = a_id
		and l_email = #{reqEmail}
		and l_password = #{reqPassword}
	</select>

</mapper>